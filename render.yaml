services:
  - type: web
    name: scaler-zoom-clone-api
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    healthCheckPath: /api/healthz
